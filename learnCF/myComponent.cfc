// myComponent.cfc

component {

    // Function to multiply three numbers
    function multiplyThreeNumbers(required numeric num1, required numeric num2, required numeric num3) {
        return num1 * num2 * num3;
    }

}
